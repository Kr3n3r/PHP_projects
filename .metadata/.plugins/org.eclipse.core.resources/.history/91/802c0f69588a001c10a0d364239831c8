<html>
<head><title>Modificar empleado</title></head>
<body>
<form>
<?php 
include_once 'utils.php';
$con = new mysqli($host, $user, $password, $dbname, $port, $socket)
or die ('Could not connect to the database server' . mysqli_connect_error());
?>
<label for='apellidos'>Apellidos:</label><input type='text' default='<?php 
$query = "select employeeNumber,firstName,lastName from employees";


if ($stmt = $con->prepare($query)) {
    $stmt->execute();
    $stmt->bind_result($employeeNumber, $firstName, $lastName);
    $stmt->close();
}?>'
</form>
</body>
</html>
<?php 
$con->close();
?>